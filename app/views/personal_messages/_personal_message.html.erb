<p>
  <%= personal_message.body %>
</p>
<p>
  sent on <strong><%= personal_message.created_at.strftime("%b %d, %Y, %l:%M%P") %></strong>
  <br>
  by <strong><%= personal_message.user.name %></strong>
</p>
<hr>
