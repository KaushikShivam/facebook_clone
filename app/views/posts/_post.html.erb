<p><%= post.content %></p>
<% if post.image_link != "" && !post.image_link.nil? %>
  <%= image_tag post.image_link  %>
<% end %>
<p><%= post.user.name %></p>
<%= link_to "Edit post", edit_post_path(post) %>
<%= link_to "Delete post", post, method: :delete %>